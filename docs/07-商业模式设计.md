# 💰 精灵1号 - 商业模式设计

> 会员订阅 + 自带 Key，两种模式并存

---

## 一、双轨模式

```
┌─────────────────────────────────────────────────────────────┐
│                      精灵1号用户                             │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          │                               │
          ▼                               ▼
┌─────────────────────┐       ┌─────────────────────┐
│   🌟 会员订阅模式    │       │   🔑 自带 Key 模式   │
│                     │       │                     │
│  • 开箱即用         │       │  • 自己配置 API Key  │
│  • 零配置           │       │  • 自己承担费用      │
│  • 按月/年付费      │       │  • 更灵活            │
│  • 平台托管模型     │       │  • 无订阅费          │
│                     │       │                     │
│  适合：普通用户     │       │  适合：开发者/企业   │
└─────────────────────┘       └─────────────────────┘
```

---

## 二、会员订阅模式

### 2.1 会员等级设计

| 等级 | 月费 | 包含内容 |
|------|------|---------|
| **免费版** | ¥0 | 每天 20 次对话，基础模型 |
| **标准版** | ¥29/月 | 每天 200 次，主流模型 |
| **专业版** | ¥99/月 | 无限对话，所有模型 + 高级功能 |
| **团队版** | ¥299/月 | 多人协作 + 企业功能 |

### 2.2 用户体验

```
新用户注册：

精灵: "欢迎使用精灵1号！🧚
      
      你可以免费体验，每天 20 次对话。
      
      想要更多？升级会员即可！
      
      [🆓 免费体验]
      [🌟 查看会员权益]"
```

### 2.3 付费用户界面

```
┌─────────────────────────────────────────────────────────────┐
│  👤 我的账户                                    专业版会员   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📊 本月使用                                                │
│  ├─ 对话次数：1,234 次                                      │
│  ├─ Agent 创建：12 个                                       │
│  └─ 任务执行：89 次                                         │
│                                                             │
│  🤖 可用模型                                                │
│  ├─ GPT-4o ✅                                              │
│  ├─ Claude 3.5 ✅                                          │
│  ├─ DeepSeek R1 ✅                                         │
│  └─ 更多...                                                │
│                                                             │
│  会员到期：2026-03-01                                       │
│  [续费] [升级] [查看账单]                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.4 平台成本控制

```
平台策略：

1. 智能模型调度
   - 简单问题 → 便宜模型（Qwen-7B）
   - 复杂问题 → 强力模型（GPT-4）
   - 控制成本同时保证体验

2. 缓存机制
   - 相似问题复用答案
   - 减少重复调用

3. 批量采购
   - 与 API 提供商谈批量价格
   - 降低单次调用成本
```

---

## 三、自带 Key 模式（BYOK）

### 3.1 适用场景

| 场景 | 说明 |
|------|------|
| **开发者** | 已经有 API Key，想用精灵的能力 |
| **企业用户** | 公司统一采购 API，要用公司的 Key |
| **高频用户** | 用量大，自己买 API 更划算 |
| **特殊模型** | 想用平台没提供的模型 |

### 3.2 用户配置界面

```
┌─────────────────────────────────────────────────────────────┐
│  🔑 我的 API Key                                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  💡 使用自己的 API Key，无需订阅会员！                       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  中转站（推荐，一个 Key 用所有模型）                  │   │
│  │                                                     │   │
│  │  ○ 硅基流动 (SiliconFlow)                           │   │
│  │    API Key: [sk-xxx...xxx] [测试] [保存]            │   │
│  │                                                     │   │
│  │  ○ OpenRouter                                       │   │
│  │    API Key: [未配置] [配置]                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  直连模型（单独配置）                                │   │
│  │                                                     │   │
│  │  ○ OpenAI: [已配置] ✅                              │   │
│  │  ○ Anthropic: [未配置]                              │   │
│  │  ○ Google: [未配置]                                 │   │
│  │  ○ 本地 Ollama: [已配置] ✅                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ⚠️ 使用自己的 Key 时，API 费用由你自己承担                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 BYOK 用户权益

| 功能 | 免费用户 | BYOK 用户 |
|------|---------|----------|
| 对话次数 | 20次/天 | **无限制** |
| 模型选择 | 基础模型 | **自己配置的所有模型** |
| Agent 创建 | 3 个 | **无限制** |
| 浏览器自动化 | 10次/天 | **无限制** |
| 服务器运维 | ❌ | ✅ |

---

## 四、混合模式

### 4.1 用户可以两者兼用

```
场景：用户订阅了标准版，但想用 GPT-4（专业版才有）

方案：
├─ 继续用标准版的额度
├─ 自己配置 OpenAI Key
└─ 用 GPT-4 时走自己的 Key

用户省钱，平台也省成本！
```

### 4.2 界面设计

```
┌─────────────────────────────────────────────────────────────┐
│  🤖 模型设置                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  当前使用：                                                 │
│                                                             │
│  ● 平台提供的模型（标准版会员）                             │
│    └─ GPT-3.5、Claude Instant、Qwen-32B                    │
│                                                             │
│  ☑️ 同时启用我的 API Key                                    │
│    └─ OpenAI GPT-4 (我的 Key) ✅                           │
│    └─ Claude 3.5 (我的 Key) ✅                             │
│                                                             │
│  模型优先级：                                               │
│  [我的 Key 优先] / [平台优先] / [智能选择]                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、定价策略

### 5.1 会员定价

| 等级 | 月付 | 年付（8折）| 核心权益 |
|------|------|-----------|---------|
| 免费 | ¥0 | - | 体验基础功能 |
| 标准 | ¥29 | ¥279/年 | 日常使用够用 |
| 专业 | ¥99 | ¥948/年 | 全部功能无限制 |
| 团队 | ¥299 | ¥2868/年 | 多人 + 企业功能 |

### 5.2 增值服务

| 服务 | 价格 | 说明 |
|------|------|------|
| 额外对话包 | ¥9/100次 | 超出套餐后购买 |
| 私有部署 | 按需报价 | 企业本地部署 |
| 定制开发 | 按需报价 | 定制 Agent/功能 |

---

## 六、用户引导流程

### 6.1 新用户首次使用

```
精灵: "欢迎使用精灵1号！🧚
      
      选择适合你的方式：
      
      ┌─────────────────────────────────┐
      │  🌟 会员订阅                    │
      │  开箱即用，无需配置             │
      │  ¥29/月起                       │
      │  [立即订阅]                     │
      └─────────────────────────────────┘
      
      ┌─────────────────────────────────┐
      │  🔑 使用自己的 API Key          │
      │  自己配置，按 API 用量付费      │
      │  无订阅费                       │
      │  [配置我的 Key]                 │
      └─────────────────────────────────┘
      
      ┌─────────────────────────────────┐
      │  🆓 先免费体验                  │
      │  每天 20 次对话                 │
      │  [开始体验]                     │
      └─────────────────────────────────┘"
```

### 6.2 免费额度用完时

```
精灵: "今天的免费额度用完啦！
      
      想继续使用？
      
      [🌟 升级会员 ¥29/月]  ← 推荐
      [🔑 配置自己的 API Key]
      [⏰ 明天再来]"
```

---

## 七、商业模式对比

### 7.1 与竞品对比

| 产品 | 会员订阅 | 自带 Key | 精灵1号 |
|------|---------|---------|--------|
| ChatGPT | ✅ $20/月 | ✅ API | ✅ 两种都有 |
| Claude | ✅ $20/月 | ✅ API | ✅ 两种都有 |
| Cursor | ✅ $20/月 | ✅ BYOK | ✅ 两种都有 |
| 豆包 | ✅ 免费/付费 | ❌ | ✅ 更灵活 |

### 7.2 精灵1号优势

```
1. 灵活选择
   - 小白用户：订阅即用
   - 高级用户：自带 Key 省钱

2. 混合使用
   - 订阅 + 自己的 Key 可以同时用
   - 按需选择最优方案

3. 透明定价
   - 会员价格清晰
   - BYOK 用户只付 API 费用
```

---

## 八、实现要点

### 8.1 技术实现

```typescript
// 判断用户应该使用哪个模型来源
async function resolveModel(userId: string, requestedModel: string) {
  const user = await getUser(userId);
  
  // 1. 检查用户是否有这个模型的自己的 Key
  const userKey = await getUserApiKey(userId, requestedModel);
  if (userKey) {
    return { source: 'user_key', key: userKey };
  }
  
  // 2. 检查用户会员等级是否包含这个模型
  if (user.membership.includes(requestedModel)) {
    return { source: 'platform', key: PLATFORM_KEY };
  }
  
  // 3. 检查免费额度
  if (user.freeQuota > 0 && isBasicModel(requestedModel)) {
    return { source: 'platform_free', key: PLATFORM_KEY };
  }
  
  // 4. 都不行，提示用户
  throw new NeedUpgradeError('请升级会员或配置 API Key');
}
```

### 8.2 计费逻辑

```typescript
// 记录使用并扣费
async function recordUsage(userId: string, model: string, tokens: number) {
  const resolution = await resolveModel(userId, model);
  
  switch (resolution.source) {
    case 'user_key':
      // 用户自己的 Key，不扣平台额度
      await logUsage(userId, model, tokens, 'user_key');
      break;
      
    case 'platform':
      // 扣会员额度
      await deductMembershipQuota(userId, tokens);
      await logUsage(userId, model, tokens, 'membership');
      break;
      
    case 'platform_free':
      // 扣免费额度
      await deductFreeQuota(userId, 1);
      await logUsage(userId, model, tokens, 'free');
      break;
  }
}
```

---

## 九、总结

| 用户类型 | 推荐模式 | 说明 |
|---------|---------|------|
| 小白用户 | 会员订阅 | 开箱即用，省心 |
| 轻度用户 | 免费版 | 够用就行 |
| 重度用户 | 自带 Key | 用量大，自己买划算 |
| 企业用户 | 自带 Key + 团队版 | 用公司的 Key，要团队功能 |
| 开发者 | 自带 Key | 灵活，可控 |

**两种模式并存，覆盖所有用户！**

---

*让用户选择最适合自己的方式*
