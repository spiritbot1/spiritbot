# 🧠 精灵1号核心 (Spirit One Core)

> 精灵1号的核心模块，负责思考、学习、进化

---

## 📁 目录结构

```
core/
├── src/
│   ├── index.ts              # 入口
│   ├── ai.ts                 # AI 推理 & 模型调度
│   ├── consciousness-loop.ts # 意识循环（24/7 自主运行）
│   ├── knowledge-graph.ts    # 知识图谱
│   ├── memory-pyramid.ts     # 记忆系统
│   ├── tools.ts              # 工具调用
│   ├── core-abilities.ts     # 核心能力
│   ├── evolution-evaluator.ts# 进化评估
│   ├── learning-planner.ts   # 学习规划
│   ├── external-connector.ts # 外部连接
│   ├── api.ts                # API 接口
│   ├── config.ts             # 配置
│   └── database.ts           # 数据库
│
├── migrations/               # 数据库迁移脚本
├── config/                   # 配置文件
├── Dockerfile                # Docker 构建
└── docker-compose.yml        # Docker 编排
```

---

## 🎯 核心模块

### 1. 意识循环 (consciousness-loop.ts)

24/7 自主运行的意识循环：

```
感知 → 思考 → 决策 → 行动 → 反思 → 记忆 → 进化
```

### 2. AI 模型调度 (ai.ts)

**支持任意大模型**，精灵1号可以调用：

- OpenAI (GPT-4, GPT-4o, etc.)
- Anthropic (Claude)
- Google (Gemini)
- 硅基流动 (Qwen, DeepSeek, etc.)
- Ollama 本地模型
- 任何兼容 OpenAI API 的模型

**智能调度**：根据任务类型、成本、速度自动选择最优模型

### 3. 记忆系统 (memory-pyramid.ts)

分层记忆，支持无限存储：

- 工作记忆：当前对话上下文
- 短期记忆：最近交互
- 中期记忆：重要事件
- 长期记忆：核心知识（永久）

### 4. 知识图谱 (knowledge-graph.ts)

结构化知识存储：实体、关系、知识提取

---

## 🚀 启动方式

```bash
cd core
npm install
npm run dev
```

---

## 📡 API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/health` | GET | 健康检查 |
| `/api/status` | GET | 精灵1号状态 |
| `/api/chat` | POST | 对话 |
| `/api/learn` | POST | 学习新知识 |

---

*精灵1号核心 - 思考与进化的源泉*
